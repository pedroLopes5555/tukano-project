config:
  target: "http://localhost:8080/rest"
  phases:
    - duration: 1
      arrivalRate: 1

scenarios:
    flow:
      - get:
          url: "/shorts/{{ userId }}/shorts" 

# IN THIS ONE WE HAVE TO REPLACE THE USERID BY THE DESIRED USER
